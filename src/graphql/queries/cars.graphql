#import { carFields } from '../fragments/car.graphql'

query Cars($orderBy: [cars_order_by!], $whereCars: cars_bool_exp, $whereUserCars: user_cars_bool_exp) {
  cars(where: $whereCars, order_by: $orderBy) {
    ...carFields
  }
  user_cars(where: $whereUserCars) {
    id
    car_id
  }
}